# Usage

```
Usage:
  > win-move.exe
      prints all windows and their associated monitors
  > win-move.exe help
      prints this help message
  > win-move.exe <window_title> <monitor_id>
      supports partial window title matching
      also supports window id matching in the format <monitor_id>-<window_index>
```